global:
  scrape_interval: 15s

storage:
  tsdb:
    out_of_order_time_window: 30m   # required for Tempo's remote-written span metrics

scrape_configs:
  - job_name: tempo
    static_configs:
      - targets: ["tempo:3200"]
